meta {
  name: Delete Template
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/v1/ai/templates/delete?name=greeting&version=v1
  body: none
  auth: bearer
}

params:query {
  name: greeting
  version: v1
}

auth:bearer {
  token: {{jwt_token}}
}

settings {
  encodeUrl: true
}
