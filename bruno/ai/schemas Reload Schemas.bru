meta {
  name: Reload Schemas
  type: http
  seq: 5
}

post {
  url: {{base_url}}/v1/ai/schemas/reload
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

settings {
  encodeUrl: true
}
